{
  "name": "@ujg-fed-a11y/federation-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@ujg-fed-a11y/playwright-artifacts": "workspace:*",
    "@ujg-fed-a11y/playwright-axe-audit": "workspace:*",
    "@playwright/test": "^1.61.1",
    "dotenv": "^17.4.2"
  }
}
